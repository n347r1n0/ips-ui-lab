<FloatingChipWheel
  items={mobileChipItems}
  activeId={activeId}
  onSelect={scrollTo}
  dock="br"
  stepDeg={36}                 // 🧮 временный фикс: чётное число визуальных клиньев — визуально стабильнее
  skin="poker"
  skinProps={{
    // ── 🎨 ПАЛИТРА (готовые наборы оттенков кольца/фона под стеклом)
    palette: 'titanium',       // 'titanium' (средний металл), 'glassRedIvory' (красно-слоновая), 'silver'

    // ── ⚪ ЦЕНТР (форма чаши)
    center: 'bezel',           // 'bezel' — светлый обод, тень от диска на обод; 'bowl' — мягкая вогнутая чаша

    // ── 🧭 ГЕОМЕТРИЯ КОЛЬЦА
    rimWidth: 30,              // толщина цветного обода (px)
    gapDeg: 1,                 // ширина тонкой разделительной линии между клиньями (в градусах)
    visualWedgeDeg: null,      // ширина «видимого» клина; null = равна stepDeg (иконки и клинья совпадают)
    phaseDeg: 0,               // тонкий сдвиг узора вокруг центра; подгоняем, чтобы иконка попадала в центр клина
    overlapDeg: 0.0,           // микроперехлёст клиньев (в градусах) от щелей/антиалиасинга

    // ── 🪟 СТЕКЛО ПОД ОБОДОМ (как влияет на фон за кольцом)
    blurPx: 8,                 // сила размытия под кольцом
    saturate: 1.12,            // насыщенность цвета фона под кольцом (1 = без изменений)
    brighten: 1.04,            // яркость фона под кольцом (1 = без изменений)

    // ── ┆ РАЗДЕЛИТЕЛИ
    showGaps: true,            // переключатель рисования тонких разделителей
    gapAlpha: 0.28,            // прозрачность разделителей (0..1)

    // ── ✨ АКТИВНАЯ ИКОНКА (мягкий «фишечный» акцент)
    activeIcon: {
      ringEnabled: true,       // включить золотую подложку под активной иконкой
      ringAlpha: 0.18,         // интенсивность этой подложки
      ringRadiusPx: 20,        // радиус подложки (px), независим от chipSize
      ringSoftPx: 10,          // мягкость растушёвки
      scale: 1.06,             // масштаб активной иконки (1.0 = без увеличения)
      glow: 0.30,              // сила наружного золотого свечения (0..1)
      insetGlow: 0.12,         // внутренний белый блик (0..1)
      glyph: 'inherit',        // 'inherit' | 'gold' | 'custom'
      activeGlyphColor: 'var(--gold, #D4AF37)', // используется, если glyph='custom'
    },

    // ── 🛠 КРОМКА ЦЕНТРА (доп. тонкая настройка)
    // если оставить undefined — берётся из выбранного пресета центра
    // cupRimThicknessPx: 12,   // толщина светлого обода чаши (px)
    // cupRimGlow: 0.80,        // «ореол» поверх кромки (0..1)
  }}
  labelClassName="text-left text-m px-2 py-1"
/>
